<% provide(:title, "Pedidos") %>
<div class="page-header">
  <div class="row">
    <div class="span9">
      <h2>Pedidos <small>Gestión de Pedidos</small></h2>
    </div>
    <div class="span3">
      <p class="text-right">
        <%= link_to "Nuevo pedido", new_consignment_path, class: "btn btn-primary btn-large" %>
      </p>
    </div>
  </div>
</div>

<!-- ul class="nav nav-tabs">
  <li class="active">
    <%= link_to "Todos", consignments_path %>
  </li>
  <li>
    <%= link_to "Enviados", consignments_path(:status => "enviados") %>
  </li>
  <li>
    <%= link_to "Recibidos", consignments_path(:status => "recibidos") %>
  </li>
</ul -->

<%= will_paginate %>

<table class="table table-striped table-bordered table-hover table-condensed">
  <thead>
    <tr>
      <th>ID</th>
      <th>Pedido</th>
      <th>Producto(s)</th>
      <th>Item(s)</th>
      <th>Total</th>
      <th>Estado</th>
      <th>Acciones</th>
    </tr>
  </thead>
  <tbody>
    <%= render @consignments %>
  </tbody>
</table>

<%= will_paginate %>