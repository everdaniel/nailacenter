<% provide(:title, "Usuarios") %>
<div class="page-header">
	<div class="row">
		<div class="span9">
			<h2>Usuarios <small>Gestión de Usuarios</small></h2>
		</div>
		<div class="span3">
			<p class="text-right">
				<%= link_to "Nuevo usuario", new_user_path, class: "btn btn-primary btn-large" %>
			</p>
		</div>
	</div>
</div>

<%= will_paginate %>

<table class="table table-striped table-bordered table-hover table-condensed">
	<thead>
		<tr>
			<th>ID</th>
			<th>Usuario</th>
			<th>Nombre(s)</th>
			<th>Email</th>
			<th>Admin?</th>
			<th>Acciones</th>
		</tr>
	</thead>
	<tbody>
		<%= render @users %>
	</tbody>
</table>

<%= will_paginate %>